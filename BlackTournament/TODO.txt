#finalize projectile (weapon) lighting
#complete map powerplant
-# collision, pickup, spawns
-# lights
-# check radiation zone damage (postponed until HUD ready)
#fix net message crash when switching maps (somethings not properly disposed!)
#fix UI focusloss mouse VS gamepad
#fix UI broken keyboard/UI input
#find solution for comboboxes and other UI dialogs -> screen filling overlay with auto close - this is the space for all dialog-like interfaces
#replace main menue->host->map textbox with dynamically filled combo-box
#remove map temple from compile
#rename states to scenes
#HUD!
-# create template
-# create weapon icons
-# create HUD specific assets
-# add
-# attach
#fix weapon change bug + duplicate weapon set tracking -> Player.OwnedWeapons VS ServerPlayer.Weapons VS ClientPlayer.???? check clean fix!
#add meta game logic & game time
#add game & chat messages
#consider clearing ui inputs on REMOVE not on ADD fixed via input inheritance
#clean ui input?
#add score window (tab)
#fix right aligned text on HUD -> Rank Label
#add ingame menue (esc)
#fix crash when exiting 2 times
#consider: update SFML to latest
#X (no) consider: removing Init from UI
#consider: removing int.toVector extension
#fix: "ghost" players on server restart
#X (not rep) fix: inspector shows main menue state when loaded into a 2nd map
#fix: connecting to invalid server causing crash instead of proper message
#update playergraphics
#add colorizer
#finalize launcher
Xfix alpha inheritance - not possible - workaround is functional
#fix redundant code in all entities
# test polygon
#fix random overload
#fix UI textbox vs console collision
#create & add launcher banner image
#fix power plant radiation zone damage values
#add damage sounds (and effects)
#add death sounds (and effects)
#finish pickup sounds (sounds already picked and added - implement, test & make sure volumes are ok)
#fix general pickup respawn times!
#fix special pickup times -> weapons 20sek big shield 35 sek
#fix balancing based on latest playtest
#fix healitem values
#force respawn
#fix drake light on turbine
#add emitters to pickups
#character customization - tint
#- fix missing tint sync
#(consider replacing some sounds with unfa sounds)
#fix sfx volumes
#fix missing implementations 4 volume settings # add to save adapter
#fix caret color not following text color
#fix initial weapon cooldown
#(add damage indicator - with sounds)
#(complete map temple)
X(env sounds?)

(consider UI Markup)
(consider Particle Markup)
(consider replacing TMX Lib)
(multiple characters?)



server game logic
# - static collisions
# - dynamic collision (dyn to dyn)

client game logic
X - static collisions (dyn to stat where dyn is owned by local)


# key(input) to enum(Action) mapping


idee: ein quasi rollensystem mittels actions realisieren - ähnlich continuous animations - gibts die nicht schon?
X idee: controller durch rollen ersetzen die am jeweiligen state hängen


# create emitter configurations for VFX

# fix collision bug: no collision / intersection between rectangle and 0 degree ray (->)

#add id-d shots & 
#pickups to serialization chain

# create game data from parsed map data?

# create basic UI elements

? add MD5 comparison against map tampering / versioning