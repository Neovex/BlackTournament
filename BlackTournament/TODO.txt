
complete input abstraction layer - key to action mapping - editable (maybe check tf3 for that)

server pure data abstraction layer - poco game data 4 serialization


server game logic
- dynamic collision (dyn to dyn)

client game logic
- static collisions (dyn to stat where dyn is owned by local)


thoughts on input
key(input) to enum(Action) mapping... CUAH


- BSP Tree Planning:
abstract collision data
BspNode:
(parent)
ChildA&B
List<CollisionBase||ICollidable>
Collision Classes:
Collision Object - implementation of a non visual collision obj
Collision Ref - ref to gfx or other rectangular col. relevant obj

Real Trails
https://sourceforge.net/p/mars-game/code/HEAD/tree/src/TrailEffects/FloatingTrail.cpp#l65
